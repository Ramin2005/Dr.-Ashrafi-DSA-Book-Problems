#include <stdio.h>

void Combination(int n, int num10, int num5, int num2)
{
    if (n = 0)
    {
        printf("\n%d %d %d", num10, num5, num2);
        return;
    }

    if (n < 2)
        return;

    
}